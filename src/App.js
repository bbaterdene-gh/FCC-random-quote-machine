import { Quote } from "./components/Quote"

export const App = () => {
  return (
    <div className="App flex justify-center items-center w-screen h-screen bg-red-300">
      <Quote />
    </div>
  )
}